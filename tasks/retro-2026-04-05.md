# Retrospective — 2026-04-05 — Toast Notifications + Generation Loading

## Metrics
| Metric | Value |
|--------|-------|
| Planned tasks | 13 (todo.md checkboxes) |
| Completed | 13 / 13 (100%) |
| Commits | 1 (single atomic commit) |
| Time span | Single session |
| Files changed | 12 (+842/-16) |
| Gate attempts | tsc: 1 pass, lint: 1 pass (0 fix cycles) |
| Blockers | 1 (linter reverted settings.tsx state removal, re-applied) |
| Rework rate | 0% (no fix commits needed) |

## What Went Well
- Clean single-commit delivery with zero gate failures
- Parallel exploration with 3 agents upfront gave complete picture of all 19 mutation flows before writing any code
- Sonner integration was straightforward — one import per file, consistent pattern
- Generation loading component reused existing Mascot component and CSS keyframe infrastructure

## What Didn't Go Well
- Linter/formatter reverted the settings.tsx state variable removal mid-edit — had to re-apply the same change. Root cause: file watcher auto-formatted between edits.
- The edit deck page (`edit/page.tsx`) was tracked as a new file (636 lines) in git because it was previously untracked — inflated the diff stat. Actual changes were ~30 lines of toast calls.

## Patterns
- **Toast integration is repetitive**: same `try { action(); toast.success() } catch { toast.error() }` pattern across 10+ files. Could extract a wrapper utility if this grows further, but YAGNI for now.
- **First retro**: no previous retros to follow up on.

## Action Items
1. **Investigate file watcher conflicts** — Linter auto-reverting changes mid-session caused a rework. Apply during: next session, check if a format-on-save hook is conflicting.
2. **Stage untracked files before feature work** — The edit page being untracked inflated the diff. Apply during: before starting any feature branch, commit or stash unrelated working changes.
3. **Consider a `useActionToast` hook** — If more mutation flows are added, a hook wrapping try/catch with toast could reduce boilerplate. Apply during: when toast count exceeds 20+ call sites.
